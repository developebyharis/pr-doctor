# Fix any issues in the following code from apps/integration/pyproject.toml:14-14

Current problems detected:
- [Python-InstalledPackagesChecker] Package `uvicorn` is not installed in the selected environment. ([object Object])

```
uvicorn>=0.52.4
```

Please fix the issues and explain what was wrong.

---

**Status:** active  **Date:** 2026-08-29

---

### 👤 User

Fix any issues in the following code from apps/integration/pyproject.toml:14-14

Current problems detected:
- [Python-InstalledPackagesChecker] Package `uvicorn` is not installed in the selected environment. ([object Object])

```
uvicorn>=0.52.4
```

Please fix the issues and explain what was wrong.

---

### 🤖 Assistant

> 🔧 called: read_file, execute_command

Let me first look at the project structure and the pyproject.toml file to understand the setup.

> 🔧 **read_file** — apps/integration/pyproject.toml

> 🔧 **execute_command** — cd apps/integration && python --version && pip show uvicorn 2>&1; uv pip show uvicorn 2>&1; which uv 2>&1

### 🤖 Assistant

`uvicorn` **is already installed** — it's present at `.venv/lib/python3.13/site-packages`. The issue is purely a **linter/IDE misconfiguration**: the Python environment selected in your editor is not pointing to the project's virtual environment at `apps/integration/.venv`.

The `pyproject.toml` itself is correct and does not need any changes. Here's what's happening and how to fix it:

---

**Root cause:** The `[Python-InstalledPackagesChecker]` warning fires when the IDE's selected Python interpreter is the system/global Python rather than the project's `.venv`. Since `uvicorn` is only installed inside the local venv (`.venv/lib/python3.13/site-packages`), the global interpreter doesn't see it.

**Fix — select the correct interpreter:**

- **VS Code / Cursor:** Open the Command Palette (`Ctrl+Shift+P`) → **"Python: Select Interpreter"** → choose the path:
  ```
  apps/integration/.venv/bin/python
  ```
  Or add this to your workspace `.vscode/settings.json`:
  ```json
  {
    "python.defaultInterpreterPath": "${workspaceFolder}/apps/integration/.venv/bin/python"
  }
  ```

- **If using `uv` to manage the venv**, you can also run the following to ensure the venv is fully synced:
  ```bash
  cd apps/integration && uv sync
  ```

No code changes are needed — `uvicorn 0.52.4` is installed and the `pyproject.toml` declaration is valid.