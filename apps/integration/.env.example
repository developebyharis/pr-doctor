
# GitHub Personal Access Token (needs repo + read:org scopes)
GITHUB_TOKEN=ghp_---------------------------

# Target GitHub repo in "owner/repo" format
GITHUB_REPO=owner/repo  # haris/repo-name

# Local FastAPI server
API_HOST=localhost
API_PORT=8000

# TinyDB file path (relative to apps/integration/)
DB_PATH=./data/pr_doctor.json

